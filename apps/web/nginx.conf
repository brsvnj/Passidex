server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # SPA: fall back to index.html so client-side state (e.g. ?invite=…) works.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
